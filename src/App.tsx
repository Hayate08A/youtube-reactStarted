import React from 'react';

import { Button } from './component/Button';

export const App = () => {
  return (
    <div>
      helloworld
      <Button title={'aaa'} />
      <Button title={'iii'} />　
      <Button title={'uuu'} />
    </div>
  );
};
